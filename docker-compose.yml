version: '3.4'

services:
  moneytracker-tg:
    image: moneytracker-tg
    build:
      context: .
      dockerfile: ./tg/Dockerfile
    command: "python3 -c 'while True: pass'"
    volumes:
      - ./tg/database:/tg/database:rw
